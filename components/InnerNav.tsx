'use client'

// InnerNav is now an alias for the shared NavClient.
// All pages use identical nav — font, chip, border, scroll glass.
import NavClient from '@/app/components/NavClient'
export default NavClient
